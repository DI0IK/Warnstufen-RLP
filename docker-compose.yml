services:
   api:
      build: .
      volumes:
         - ./config.json:/config.json
         - ./data:/data
         - ./sslcert:/certs
         - ./app:/app/app
      ports:
         - '80:80'
         - '443:443'
         - '8080:8080'
      environment:
         - 'NODE_ENV=production'
